<?xml version="1.0"?>

<package format="2">
    <name>ignition-common3</name>
    <version>3.0.0</version>
    <description>ignition-common</description>
    <maintainer email="info@openrobotics.org">OSRF</maintainer>
    <license>APACHE-2.0</license>

    <depend>ignition-cmake2</depend>
    <depend>ignition-tools</depend>

    <depend>libavcodec-dev</depend>
    <depend>libavdevice-dev</depend>
    <depend>libavformat-dev</depend>
    <depend>libavutil-dev</depend>
    <depend>libfreeimage-dev</depend>
    <depend>libgts-dev</depend>
    <depend>libprotobuf-dev</depend>
    <depend>libprotoc-dev</depend>
    <depend>libswscale-dev</depend>
    <depend>libtinyxml2-dev</depend>
    <depend>uuid-dev</depend>

</package>
